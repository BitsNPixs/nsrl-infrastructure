// @ts-check
import { defineConfig } from "astro/config";

export default defineConfig({
  // Update this before deploying — it is used for the canonical + og:url tags.
  site: "https://nsrlinfrastructure.com",
});
